create table orders(symbol text,
                    side text,
                    type text,
                    order_price numeric,
                    order_size numeric,
                    actual_price numeric,
                    actual_amount numeric,
                    timestamp timestamp);